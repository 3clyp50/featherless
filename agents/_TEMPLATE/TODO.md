# <scope-name>

> **Subagent:** clone this directory to `agents/<your-scope>/` and fill in. Read `../README.md` and `../../PLAN.md` §11 first.

## Contract (do not modify)

- **Writable:** `<exact list from PLAN §11>`
- **Read-only context:** `<exact list from PLAN §11>`
- **Acceptance:** `<exact criteria from PLAN §11>`
- **Source of truth:** `../../PLAN.md` §11, `../../TODO.md` (relevant block), `../../../HERO_PATIENT.md` and `../../../CITATIONS.md` where applicable

## Mirrored tasks

> Copy verbatim from `../../TODO.md` every line tagged with this scope. Tick as you go, not at the end.

- [ ] <copied line>
- [ ] ...

## Decisions log

> One-line rationale per non-obvious choice. Future-you will thank present-you. Append-only.

- `YYYY-MM-DD HH:MM` · <decision> · <one-line rationale>

## Open contract questions

> Anything that requires human or judge resolution. Tag `block` if you cannot proceed without an answer, `non-block` otherwise. Do not invent answers.

- <question> — `block` / `non-block`

## Output

> Final state at hand-off. Files created or modified, summary, anything the judge needs to know.

- **Files:** `<list>`
- **Summary:** `<one paragraph of what was decided and why>`
- **Open questions:** `<copied from above>`
